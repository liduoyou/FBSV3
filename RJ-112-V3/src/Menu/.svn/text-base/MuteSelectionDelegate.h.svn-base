#ifndef MUTESELECTIONDELEGATE_H
#define MUTESELECTIONDELEGATE_H

#include <QStyledItemDelegate>

class MuteSelectionDelegate : public QStyledItemDelegate
{
    Q_OBJECT
public:
    explicit MuteSelectionDelegate(QObject *parent = 0);
    void paint(QPainter *painter, const QStyleOptionViewItem &option, const QModelIndex &index) const;

signals:

public slots:

};

#endif // MUTESELECTIONDELEGATE_H
